{
  "name": "@shadowtalk/cli",
  "version": "1.0.0",
  "description": "ShadowTalk CLI — sovereign local-first AI in your terminal",
  "type": "module",
  "bin": {
    "shadowtalk": "./bin/shadowtalk.js",
    "st": "./bin/shadowtalk.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "test": "vitest --run",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.8.3",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": ">=18"
  }
}
